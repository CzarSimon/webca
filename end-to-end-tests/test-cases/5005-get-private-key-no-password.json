{
  "name": "Get certificate private key with no password given",
  "request": {
    "method": "GET",
    "path": "/api/v1/certificates/${rootCA.id}/private-key",
    "headers": {
      "Authorization": "Bearer ${admin.token}"
    }
  },
  "response": {
    "status": 400
  }
}
